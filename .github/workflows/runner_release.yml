name: runner-release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: "10.28.0"

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Runner Tests
        run: pnpm -C packages/runner test

      - name: Bundle Runner (single file)
        run: pnpm -C packages/runner bundle

      - name: Checksums
        run: |
          cd packages/runner/dist
          sha256sum flux-runner.mjs > flux-runner.mjs.sha256

      - name: GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            packages/runner/dist/flux-runner.mjs
            packages/runner/dist/flux-runner.mjs.sha256
